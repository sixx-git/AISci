# ==================== Mock / Real API Mode ====================
# true → 使用 src/data/mockData.ts 中的模拟数据
# false → 必须调用真实后端 API，失败时显示错误提示
VITE_USE_MOCK=true

# ==================== API Base URL ====================
# 默认本地后端
VITE_API_BASE_URL=http://localhost:8000